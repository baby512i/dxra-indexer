# Server Configuration
PORT=8000
NODE_ENV=development

# Helius Webhook Configuration
# Option 1: Use Webhook ID (recommended - found in Helius dashboard)
HELIUS_WEBHOOK_ID=your_HELIUS_WEBHOOK_ID

# Option 2: Use Webhook Secret (if you have one instead)
# HELIUS_WEBHOOK_SECRET=your-webhook-secret-here

# Note: You can use either webhook ID or secret, or leave both empty for development
